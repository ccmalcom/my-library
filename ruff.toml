# Ruff config for MyLibrary — https://docs.astral.sh/ruff/
line-length = 100

[lint]
select = ["E", "F", "W", "I"]   # pyflakes, pycodestyle, import sorting
ignore = ["E501"]               # don't fail on long lines (formatter's job)
exclude = ["alembic/versions"]  # autogenerated migrations
