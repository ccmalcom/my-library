fastapi>=0.110
python-multipart>=0.0.9
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
httpx>=0.27
pandas>=2.2
python-dotenv>=1.0
anthropic>=0.40
typer>=0.12
pytest>=8.0

# --- web distribution (multi-tenant hosting) ---
# Postgres driver for the hosted Supabase DB. Local dev still defaults to SQLite,
# so these are only exercised when DATABASE_URL is set.
psycopg[binary]>=3.1        # Postgres driver (sync SQLAlchemy)
alembic>=1.13               # schema migrations (replaces init_db drop+recreate in prod)
pyjwt[crypto]>=2.8          # verify Supabase-issued JWTs
cryptography>=42.0          # AES-256-GCM encryption of per-user Anthropic keys
